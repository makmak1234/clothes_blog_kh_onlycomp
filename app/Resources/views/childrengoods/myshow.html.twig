{% extends 'base.html.twig' %}

{% block body %}
    <h1>Admin\childrenGoods</h1>

    {#{{ dump(admin_childrenGood.childrenGoodsSizeNumber.owner.childrenGoodsSizeNumber.snapshot[0].sizeGoods.size) }}#}
    childrenGood{{ dump(childrenGood) }}
    {#childrenGoodMy{{ dump(childrenGoodMy) }}
    childrenGoodId{{ dump(childrenGoodId) }}
    childrenGoodPer{{ dump(childrenGoodPer) }}#}
    
    {#<table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin_childrenGood.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ admin_childrenGood.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_childrengoods_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_childrengoods_edit', { 'id': admin_childrenGood.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>#}
{% endblock %}
