{% extends 'base.html.twig' %}

{% block stylesheets %}
     <link href="{{ asset('bundles/app/childrengoods/indexUser.css') }}" rel="stylesheet" />
     <script src="{{ asset('bundles/app/childrengoods/indexUser.js') }}" type="text/javascript"></script>
{% endblock %}

{% block body %}
    {#myServiceVarAll = 
    {{ dump(myServiceVarAll) }}
    myServiceVar = 
    {{ dump(myServiceVar) }}
    <h1>childrenGoods</h1>

    {{ dump(childrenGoods[15].childrenGoodsSizeNumber.snapshot[0].childrenGoodsColorNumber.snapshot[0].image.path) }}
    {{ dump(childrenGoodsCategory[0].image.path) }}
    {{ dump(childrenGoods[0].childrenGoodsCategory.childrenGoodsSubcategory.snapshot) }}
    {{ dump(childrenGoodsCategory) }}
    {{ dump(childrenGoodsCategory[0].childrenGoodsSubcategory.snapshot) }}#}
    
    <b>Все категории</b>
    <br><br>

    {% for childrenGoodCategory in childrenGoodsCategory %}

        {#{{dump(childrenGoodCategory.image)}}#} 

        {#<img class="show__img" src="{{ asset('uploads/documents/' ~ childrenGoodCategory.image.path) }}">#}
        {% set imgLink = asset('uploads/documents/' ~ childrenGoodCategory.image.path) %}
        {% set idDiv = "category" ~ loop.index0 %}
        <div id='{{ idDiv }}' class="category">
            <script>backgrCat('{{ idDiv }}', '{{ imgLink }}')</script>
            {{ childrenGoodCategory.title }}<br>
            {% for childrenGoodSubcategory in childrenGoodCategory.childrenGoodsSubcategory.snapshot %}
                <a href="{{ path('cat_sub_show', { 'children_goods_category_id': childrenGoodCategory.id , 'children_goods_subcategory_id': childrenGoodSubcategory.id }) }}">{{ childrenGoodSubcategory.title }}</a>
                <br>
                {# 'children_goods_category_id': childrenGoodCategory.id , 'children_goods_subcategory_id': childrenGoodSubcategory.id #} 
                
            {% endfor %} 
            <br>
        </div>
    {% endfor %}

    <br>
    {#<b><a href="{{ path('bag_register') }}">Купить</a></b>#}

{% endblock %}
