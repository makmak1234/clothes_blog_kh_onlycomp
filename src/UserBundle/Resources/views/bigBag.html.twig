
    {#<h1>Корзина</h1>
    
    idarr
    {{ dump(idarr) }}<br>

    childrenGoods
    {{ dump(childrenGoods) }}<br>

    nid
    {{ dump(nid) }}<br>

    id = {{id}}

    <b><a href="{{ path('index_user') }}">Вернуться в магазин</a>#}

    {#{{ dump(childrenGoods[0].childrenGoodsSizeNumber.snapshot[0].size.size) }}
    {{ dump(childrenGoods[0].childrenGoodsSizeNumber.snapshot[0].childrenGoodsColorNumber.snapshot[0].color.color) }}#}

    {{ dump(sizeTitle) }}
    {{ dump(colorTitle) }}
    {{ dump(sizearr) }}
    {{ dump(colorarr) }}

{% if bigBagDisp == 'block' %} 
    <table class="goodsbasketfont m-cart-full">
		<tr><td colspan= "4">Ваша корзина:<td></tr>
		<tr>
			<th>Товар</th>
			<th>Размер</th>
			<th>Цвет</th>
			<th>Цена за ед</th>
			<th>Кол-во</th>
			<th>Цена за все</th>
			<th></th>
		</tr>
		{% for childrenGood in childrenGoods %}
		<tr>
			<td>{{ childrenGood.title }}</td>
			<td>
				{#{% if childrenGood.childrenGoodsSizeNumber.snapshot[sizearr[loop.index0]].size.size is defined %}
					{{ childrenGood.childrenGoodsSizeNumber.snapshot[sizearr[loop.index0]].size.size }}
				{% endif %}#}
				{{ sizeTitle[loop.index0] }}
			</td>
			<td>
				{#{% if childrenGood.childrenGoodsSizeNumber.snapshot[sizearr[loop.index0]].childrenGoodsColorNumber.snapshot[colorarr[loop.index0]].color.color is defined %}
					{{ childrenGood.childrenGoodsSizeNumber.snapshot[sizearr[loop.index0]].childrenGoodsColorNumber.snapshot[colorarr[loop.index0]].color.color }}
				{% endif %}#}
				{{ colorTitle[loop.index0] }}
			</td>
			<td>{{ childrenGood.priceGoods.rub }}</td>
			<td>{{ nid[loop.index0] }}</td>
			<td>{{ priceone[loop.index0] }}</td>
			<td><img id="good_click_id" src="{{ asset('bundles/app/user/elements/delete_16x16.png')}}" onclick="goodbasketdel({{ childrenGood.id }}, {{ sizearr[loop.index0] }}, {{ colorarr[loop.index0] }}, 'true', 'ajax_bag_user')"></td>
		</tr>		
		{% endfor %}
		<tr><td colspan= "5">К оплате: {{ priceall }}</td></tr>{#$prsite#}
		<tr>
			<td colspan= "3"><a class="buylab" href="{{ path('bag_register') }}">Оформить</a></td>{#$dirbasbi#}
			<td colspan= "2"><a class="goodsbasketclear" href="#" onclick="goodbasketcheck('-1', 'false', 'ajax_bag_user')">Очистить корзину</a></td>
			{#<label class="goodsbasketclear" class="good_click" good-id=-1 mclon="false"><input type="hidden" />Очистить корзину</label>onclick="goodsbasketcheck('-1', 'FALSE', '')"#}
		</tr>
	</table>
{% endif %}

	{#{{ render(controller(
		'AppBundle:Article:recentArticles',
		{ 'max': 3 }
	)) }}#}

